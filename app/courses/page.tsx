import { BlockPage } from "@/components/app-courses";
import { SharedLayout } from "@/components/shared-layout";

export default function StudyRoomPage() {
  return (
    <SharedLayout>
      <BlockPage />
    </SharedLayout>
  );
}